            // struct Production newProduction;
            // newProduction.lhs=extraElement;

            // char new_string[20];
            // strncpy(new_string,deleted_production.lhs+1,strlen(deleted_production.lhs)-2);
            // new_string[strlen(new_string)]=extraElement;
            // new_string[strlen(new_string)]='\0';
            // addProduction(newProduction.lhs,new_string);
            // printf("\n");
            // printGrammar();
